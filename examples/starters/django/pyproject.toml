[project]
name = "citry-example-django"
version = "0.1.0"
description = "Run the Citry Project Explorer with Django"
requires-python = ">=3.10,<4"
dependencies = [
  "citry>=0.4.6,<0.5",
  "django>=5.2,<7",
]

[dependency-groups]
dev = [
  "citry-lsp>=0.1,<0.2",
  "pytest>=9.0,<10",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
