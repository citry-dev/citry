# Tree

`CTree` renders an accessible application tree and `CTreeItem` declares its
hierarchical Items.

The authoritative contract is
[`docs/design/ui_components/tree.md`](../../../../../../../docs/design/ui_components/tree.md).
Public usage lives in [`api.md`](api.md), and fixed reference data lives in
[`api.yml`](api.yml).
