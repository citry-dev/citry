# Rust toolchain configuration
[toolchain]
# Vendored Ruff 0.16.2 requires rustc/cargo v1.95 or higher.
# This repository tracks nightly, which satisfies that minimum automatically.
# The channel below is selected whenever cargo/rustc runs in this directory.
channel = "nightly"
components = ["rustfmt", "clippy"]
