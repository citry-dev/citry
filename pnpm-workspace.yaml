# Node packages in this monorepo. The root package.json holds only dev
# tooling (the pinned pyright the check gate runs). Publishable Node
# packages will live under packages/js/, while private contributor tools
# such as the Citry UI Storybook spike can join the workspace directly.
packages:
  - packages/js/*
  - packages/py/citry_ui/storybook
