# Node packages in this monorepo. The root package.json holds only dev
# tooling (the pinned pyright the check gate runs); publishable Node
# packages will live under packages/js/ (nothing there yet, so the glob
# matches nothing today and future packages slot in without config changes).
packages:
  - packages/js/*
